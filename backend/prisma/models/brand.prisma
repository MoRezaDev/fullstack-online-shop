model Brand {
    id String @id @default(cuid())

    title String
    title_fa String
    url String

    categories Category[]

    products Product[]

    sellers Seller[]

    seo Seo?

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}