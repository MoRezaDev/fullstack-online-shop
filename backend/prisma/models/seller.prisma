model Seller {
    id String @id @default(cuid())

    title String
    title_fa String
    url String

    products Product[]

    brands Brand[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}