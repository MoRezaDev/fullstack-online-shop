model Seller {
    id String @id @default(cuid())

    title String
    title_fa String
    url String

    products Product[]

    brands Brand[]

    buyers BuyerDetail[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}