model Payment {
    id String @id @default(cuid())

    amount Int
    date String
    is_user_paid Boolean @default(false)
    status String @default("pending")
    successful Boolean @default(false)

    orderId String
    order Order @relation(fields: [orderId],references: [id],onDelete: Cascade)
}