model User {
    id String @id @default(cuid())

    mobile String @unique
    full_name String?
    email String?
    password String?
    role String @default("user")
    
    address Address[]
    cart Cart?
    otp Otp?
    // comments Comment[]
    // orders Order[]

}